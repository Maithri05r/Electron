# Electron + React + TypeScript Base Workspace

Clean starter template for a desktop app using Electron + React + TypeScript.

## Setup
```
npm install
cd frontend && npm install && cd ..
npm run dev
```